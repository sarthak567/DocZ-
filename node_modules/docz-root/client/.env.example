# Production / Render: full URL to API, must end with /api (paths are /documents/..., /users/...)
# Example: https://docz-api-xxxx.onrender.com/api
# Local dev: leave unset to use Vite proxy (/api)
VITE_API_URL=
